import 'package:flutter/material.dart';

class AfterPayment extends StatelessWidget {
  const AfterPayment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('After payment')));
  }
}
